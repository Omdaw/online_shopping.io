import { TokenInterceptorDirective } from './token-interceptor.directive';

describe('TokenInterceptorDirective', () => {
  it('should create an instance', () => {
    const directive = new TokenInterceptorDirective();
    expect(directive).toBeTruthy();
  });
});
